/**
 * HanGarden 콘텐츠 생성 파이프라인의 핵심 타입 정의입니다.
 *
 * Codex CLI가 생성 요청 JSON을 해석할 때 이 파일을 함께 읽는다면,
 * `ContentSettings`의 각 key가 단순 데이터가 아니라 사용자의 콘텐츠 의도를 표현하는 값이라는 점을 기준으로 삼아야 합니다.
 *
 * 중요한 해석 원칙:
 * - 원시 enum 값(`intro`, `kiosk`, `children` 등)은 관리자/시스템용 값입니다. 방문자 화면에 그대로 노출하지 않습니다.
 * - `ContentSettings`의 모든 값은 페이지 구조, 문장 언어, 정보 밀도, 상호작용 방식, 화면 길이 중 하나에 반영되어야 합니다.
 * - `PlantRecord`는 목업 DB에서 온 원본 식물 데이터입니다. 원본 데이터가 한국어일 수 있으므로, 요청 언어가 한국어가 아닐 때는 그대로 렌더링하지 말고 자연스러운 대상 언어 문장으로 재작성해야 합니다.
 */

/**
 * 생성 마법사의 난이도/세부도 선택입니다.
 *
 * - `general`: 사용자가 최소 설정만 선택합니다. 빠른 생성 경로이므로 기본값이 많이 사용됩니다.
 * - `advanced`: 사용자가 단말기, 현장, 강조 소재, 톤, 레이아웃 방식 등 세부 조건을 직접 지정합니다.
 */
export type CreationMode =
  | 'general'
  | 'advanced';

/**
 * 콘텐츠 유형입니다. 생성 페이지의 가장 강한 구조적 기준입니다.
 *
 * 다른 설정값이 많더라도 이 값이 페이지의 주 경험을 결정합니다.
 * 예를 들어 `intro`는 소개 페이지여야 하며, `mission`처럼 행동 과제를 만들거나 `quiz`처럼 문제 풀이 중심으로 바뀌면 안 됩니다.
 */
export type TemplateType =
  /** 식물이 무엇인지, 지금 무엇을 보면 좋은지, 왜 의미가 있는지 알려주는 간결한 소개 페이지입니다. */
  | 'intro'
  /** 식물을 주인공 또는 해설 대상으로 삼아 짧은 이야기 흐름을 만드는 페이지입니다. */
  | 'storytelling'
  /** 질문과 선택지, 정답/해설을 중심으로 관람객 참여를 만드는 페이지입니다. */
  | 'quiz'
  /** 현장에서 수행할 수 있는 안전한 관찰 과제를 제시하는 페이지입니다. */
  | 'mission'
  /** 관람객이 순서대로 확인할 수 있는 관찰 체크 항목을 제공하는 페이지입니다. */
  | 'checklist';

/**
 * 페이지의 목적입니다. 같은 식물/템플릿이라도 카피의 초점과 CTA 성격을 바꿉니다.
 */
export type PagePurpose =
  /** 균형 잡힌 기본 해설입니다. 특정 행동 유도보다 이해를 우선합니다. */
  | 'general'
  /** 학습 목표가 있는 설명입니다. 용어는 명확하게, 이유/원리 설명을 조금 더 포함합니다. */
  | 'education'
  /** 직접 참여, 관찰, 수행을 유도합니다. 단, 템플릿이 intro라면 행동 과제는 최소화합니다. */
  | 'experience'
  /** 보전, 생태 감수성, 참여 메시지를 강조합니다. 과장된 공포 표현은 피합니다. */
  | 'campaign'
  /** 전시/기관/공간의 매력을 소개합니다. 홍보성이 있더라도 식물 정보가 중심이어야 합니다. */
  | 'promotion'
  /** 관람 동선을 돕습니다. 위치 안내와 다음 행동이 들어갈 수 있지만 템플릿 본질을 넘지 않습니다. */
  | 'route';

/**
 * 주 관람객입니다. 문장 난이도, 정보 밀도, 버튼 크기, 설명 방식에 반영합니다.
 */
export type Audience =
  /** 어린이/가족 관람객입니다. 짧은 문장, 쉬운 어휘, 안전한 행동 안내가 필요합니다. */
  | 'children'
  /** 일반 성인 관람객입니다. 지나치게 유치하지 않은 전시 해설 톤이 적합합니다. */
  | 'adults'
  /** 외국인 관람객입니다. 문화적 맥락을 조금 더 설명하고 로컬 용어를 풀어써야 합니다. */
  | 'foreigners';

/**
 * 화면 표시 언어입니다.
 *
 * 가장 중요한 규칙:
 * - `ko`가 포함되면 한국어를 기본 UI/본문 언어로 사용합니다.
 * - `ko`가 포함되지 않으면 한국어 mock DB 문자열을 그대로 화면에 출력하지 않습니다.
 * - 여러 언어가 함께 선택되면 첫 번째 언어를 주언어로 보고, 나머지는 보조 표기 정도로만 제한하는 것이 안전합니다.
 */
export type Language =
  /** 한국어 */
  | 'ko'
  /** 영어 */
  | 'en'
  /** 일본어 */
  | 'ja'
  /** 중국어 */
  | 'zh';

/**
 * 관람 계절입니다. 식물의 `seasonHighlights`와 화면의 정보 우선순위에 반영합니다.
 */
export type Season =
  /** 봄 관찰 포인트를 우선합니다. */
  | 'spring'
  /** 여름 관찰 포인트를 우선합니다. */
  | 'summer'
  /** 가을 관찰 포인트를 우선합니다. */
  | 'autumn'
  /** 겨울 관찰 포인트를 우선합니다. */
  | 'winter'
  /** 특정 계절을 고정하지 않고 대표 관찰 포인트를 사용합니다. */
  | 'auto';

/**
 * 목표 체험 시간입니다. 콘텐츠 길이와 화면 밀도를 결정하는 강한 제약입니다.
 *
 * 짧은 시간일수록 페이지는 더 짧고 명료해야 합니다.
 */
export type EstimatedTime =
  /** 한눈에 읽는 초단기 안내입니다. 핵심 한 문장과 1~2개 사실만 적합합니다. */
  | '10sec'
  /** 빠른 스캔용 안내입니다. 첫 화면 안에서 대부분의 내용을 이해해야 합니다. */
  | '30sec'
  /** 짧은 현장 체험입니다. 모바일은 약간의 스크롤이 가능하지만 kiosk는 한 화면에 담아야 합니다. */
  | '1min'
  /** 조금 더 긴 해설/체험입니다. 그래도 템플릿 목적을 벗어난 장황한 페이지는 피합니다. */
  | '3min';

/**
 * 배포 단말기/사용 환경입니다. 레이아웃과 상호작용 가능 여부를 결정하는 가장 중요한 값입니다.
 *
 * 현재 생성 마법사의 선택지는 `kiosk`, `mobile`, `staticPoster`입니다.
 */
export type DeploymentUse =
  /** FHD 전시 키오스크입니다. 스크롤 불가를 전제로 한 화면 안에 핵심 경험을 넣어야 합니다. */
  | 'kiosk'
  /** 모바일 폰입니다. 세로 스크롤 가능, 터치 영역은 충분히 크게 만들어야 합니다. */
  | 'mobile'
  /** 정적 포스터입니다. 상호작용 없이 한 페이지 안에 읽히는 포스터형 React 렌더링이어야 합니다. */
  | 'staticPoster';

/**
 * 콘텐츠가 사용될 현장 맥락입니다.
 * 공간감, 관찰 거리, 안전 안내, 배경 설명에 영향을 줍니다.
 */
export type FieldLocation =
  /** 온실. 실내/습도/희귀식물 전시 맥락이 강합니다. */
  | 'greenhouse'
  /** 정원. 일반 관람 동선과 가까운 식재 공간입니다. */
  | 'garden'
  /** 야외 정원. 날씨와 계절 변화, 넓은 관찰 거리를 고려합니다. */
  | 'outdoorGarden'
  /** 숲길. 걷는 흐름, 자연림, 안전한 거리 관찰을 고려합니다. */
  | 'forestTrail'
  /** 공원. 대중적이고 가벼운 설명이 적합합니다. */
  | 'park';

/**
 * 강조할 콘텐츠 소재입니다.
 *
 * 이 값은 정보 우선순위를 조정하지만, `template`의 본질을 바꾸면 안 됩니다.
 * 예를 들어 `intro + comparison`이라고 해서 비교 미션 페이지가 되면 안 됩니다.
 */
export type FocusTopic =
  /** 잎, 꽃, 열매, 수피, 형태 등 눈으로 확인할 수 있는 생김새입니다. */
  | 'appearance'
  /** 서식 환경, 곤충과의 관계, 계절 변화 등 생태적 맥락입니다. */
  | 'ecology'
  /** 이름, 학명, 일반명의 의미입니다. 근거 없는 어원이나 전설을 만들지 않습니다. */
  | 'nameOrigin'
  /** 사람과 식물의 관계, 상징성, 전시/문화 맥락입니다. DB 근거 범위 안에서만 사용합니다. */
  | 'cultureHistory'
  /** 식물의 활용입니다. 약효/식용/민간요법은 근거 없이 만들지 않습니다. */
  | 'usage'
  /** 보전 가치, 생태계 보호, 관람 예절입니다. 과장된 위협 표현은 피합니다. */
  | 'conservation'
  /** 다른 식물과의 비교입니다. 비교가 템플릿을 침범하지 않도록 짧은 보조 문장으로 제한합니다. */
  | 'comparison'
  /** 흥미로운 사실입니다. 재미를 주되 과학적으로 근거 없는 내용을 만들지 않습니다. */
  | 'funFacts';

/**
 * 추가 기능 옵션입니다.
 * 현재 PoC에서는 사용하지 않는 옵션입니다.
 */
export type FeatureOption =
  /** 음성 해설 옵션입니다. 실제 오디오 재생 구현 없이 안내 버튼/표시 정도로 반영할 수 있습니다. */
  | 'voiceGuide'
  /** 질문답변 AI 옵션입니다. */
  | 'qaAi'
  /** 유사 식물 카드 옵션입니다. */
  | 'similarPlantCards';

/**
 * 문장 톤입니다. 같은 정보라도 표현 방식과 밀도를 바꿉니다.
 */
export type Tone =
  /** 친근한 해설사 톤입니다. 부드럽고 안내 중심입니다. */
  | 'friendly'
  /** 동화/캐릭터 톤입니다. 어린이 대상에 적합하지만 사실을 꾸며내면 안 됩니다. */
  | 'fairytale'
  /** 박물관/전시 해설 톤입니다. 정돈되고 차분하며 성인 관람객에게 적합합니다. */
  | 'museum'
  /** 감성 스토리텔링 톤입니다. 여운은 주되 과장된 서사를 만들지 않습니다. */
  | 'emotional'
  /** 전문가 톤입니다. 학명/분류/생태 설명을 조금 더 정확하게 다룹니다. */
  | 'expert'
  /** 캠페인 톤입니다. 보전 행동과 메시지를 강조합니다. */
  | 'campaign';

/**
 * 스토리텔링 템플릿에서만 의미가 있는 이야기 시나리오입니다.
 *
 * `template !== 'storytelling'`이면 이 값은 생성 결과에 영향을 주지 않아야 합니다.
 */
export type StoryScenario =
  /** 사라진다면 어떤 의미를 잃는지 생각하게 하는 시나리오입니다. 공포보다 돌봄과 희망을 우선합니다. */
  | 'extinction'
  /** 하루 동안 식물 주변에서 벌어지는 관찰 가능한 변화를 따라갑니다. */
  | 'dayInLife'
  /** 원산/역사/계절 변화를 시간 흐름으로 연결합니다. */
  | 'timeTravel'
  /** 기후 변화와 생존, 서식지 보전을 연결합니다. */
  | 'climateSurvival'
  /** 이름과 보이는 특징을 연결합니다. 근거 없는 어원은 만들지 않습니다. */
  | 'nameSecret';

/**
 * 레이아웃 생성 방식입니다.
 */
export type LayoutId =
  /** Codex CLI가 요청값과 목업 DB를 바탕으로 새 React 레이아웃 파일을 생성합니다. */
  | 'generated'
  /** 생성형 AI를 사용하지 않고 템플릿별 기본 레이아웃 컴포넌트를 재사용합니다. */
  | 'default';

/**
 * 콘텐츠 게시 상태입니다. 현재는 로컬 파일 기반 데모 상태값입니다.
 */
export type ContentStatus =
  /** 관리자 검토/수정 중인 초안입니다. */
  | 'draft'
  /** 게시 상태로 표시되는 콘텐츠입니다. */
  | 'published';

/**
 * 식물 이미지 메타데이터입니다.
 * 방문자 페이지에서는 `url`과 `alt`를 중심으로 사용하고, 출처/라이선스는 보조 정보로만 작게 표시합니다.
 */
export interface PlantImage {
  /** 실제 렌더링할 이미지 URL입니다. */
  url: string;
  /** 이미지 대체 텍스트입니다. */
  alt: string;
  /** 이미지 출처 이름입니다. */
  source: string;
  /** 출처 상세 URL입니다. 방문자 경험의 중심에 노출하지 않습니다. */
  sourceUrl: string;
  /** 라이선스 정보입니다. 필요할 때만 작은 보조 정보로 사용합니다. */
  license: string;
}

/**
 * 목업 DB의 식물 레코드입니다.
 *
 * 이 데이터는 향후 실제 수목원 DB로 대체될 수 있는 원본 데이터 포맷입니다.
 * 생성형 레이아웃은 이 값을 그대로 덤프하지 말고, 요청 언어와 템플릿 목적에 맞게 재구성해야 합니다.
 */
export interface PlantRecord {
  /** DB 내부 식별자입니다. 방문자 화면에 그대로 표시하지 않습니다. */
  id: string;
  /** 식물 형태 분류입니다. 화면에는 "나무", "관목", "초본" 등 자연어로 변환합니다. */
  category: 'tree' | 'shrub' | 'herb';
  /** 한국어 이름입니다. 요청 언어가 한국어가 아닐 때는 그대로 제목에 섞지 않는 편이 안전합니다. */
  koreanName: string;
  /** 학명입니다. 보조 정보로 표시할 수 있습니다. */
  scientificName: string;
  /** 영어권 일반명 등 대중적인 이름입니다. 영어 요청에서는 이 값을 제목/본문에 우선 사용할 수 있습니다. */
  commonName: string;
  /** 과명입니다. 전문 정보 또는 빠른 사실 카드에 적합합니다. */
  family: string;
  /** 원산/분포 설명입니다. 문화/역사나 스토리텔링의 근거가 될 수 있습니다. */
  origin: string;
  /** 서식 환경입니다. 현장 안내와 생태 설명의 근거가 됩니다. */
  habitat: string;
  /** 식물 크기입니다. 빠른 사실 또는 관찰 거리 안내에 사용할 수 있습니다. */
  size: string;
  /** 개화 또는 주요 관찰 시기입니다. 계절 안내에 사용할 수 있습니다. */
  floweringSeason: string;
  /** 식물의 주요 특징입니다. 관찰 포인트, 퀴즈 선택지, 체크 항목의 근거가 됩니다. */
  features: string[];
  /** 보전/생태/관람 예절 메시지의 원천입니다. */
  conservationMessage: string;
  /** 안전한 관찰 행동 목록입니다. 미션/체크리스트에서 특히 유용합니다. */
  observationTips: string[];
  /** 계절별 하이라이트입니다. `ContentSettings.season`에 맞춰 우선 반영합니다. */
  seasonHighlights: Record<Season, string>;
  /** 대표 이미지와 출처 정보입니다. */
  image: PlantImage;
  /** 유사 식물 ID 목록입니다. 원본 ID이므로 그대로 렌더링하지 않습니다. */
  similarPlantIds: string[];
}

/**
 * 생성 마법사가 수집하고 CLI에 전달하는 핵심 설정 객체입니다.
 *
 * 이 객체의 각 key는 사용자가 의도적으로 선택한 페이지 생성 조건입니다.
 * 생성형 AI는 특정 값만 골라 쓰지 말고, 모든 값이 화면 결과에 반영되었는지 확인해야 합니다.
 */
export interface ContentSettings {
  /** 일반/고급 생성 경로입니다. 고급 생성일수록 아래 세부 옵션의 의미를 더 강하게 반영합니다. */
  mode: CreationMode;

  /** 관리자 목록과 파일 저장에서 쓰는 콘텐츠 이름입니다. 밑줄/날짜가 포함될 수 있으므로 방문자용 H1로 그대로 쓰지 않습니다. */
  contentName: string;

  /** 선택한 식물의 DB ID입니다. 실제 식물 정보는 `mock_db.json`의 같은 ID를 가진 `PlantRecord`에서 가져옵니다. */
  plantId: string;

  /** 콘텐츠 유형입니다. 페이지 구조를 결정하는 최우선 값입니다. */
  template: TemplateType;

  /** AI 생성 레이아웃을 만들지, 기본 레이아웃을 재사용할지 결정합니다. */
  layoutId: LayoutId;

  /** 페이지 목적입니다. 카피의 방향, 메시지 강도, 정보 우선순위에 반영합니다. */
  purpose: PagePurpose;

  /** 주요 관람객입니다. 문장 난이도, 설명 밀도, 상호작용 크기에 반영합니다. */
  audience: Audience[];

  /** 출력 언어입니다. 선택된 언어와 다른 원본 DB 문자열이 섞이지 않도록 주의해야 합니다. */
  languages: Language[];

  /** 관람 계절입니다. 식물의 계절별 하이라이트와 시각적 분위기에 반영합니다. */
  season: Season;

  /** 목표 체험 시간입니다. 페이지 길이와 정보량을 강하게 제한합니다. */
  estimatedTime: EstimatedTime;

  /** 배포 단말기입니다. `kiosk`는 특히 스크롤 없는 한 화면 제약을 의미합니다. */
  deploymentUse: DeploymentUse;

  /** 현장 위치입니다. 공간 맥락과 안전한 관찰 안내에 반영합니다. */
  fieldLocation: FieldLocation;

  /** 강조 콘텐츠 소재입니다. 템플릿 구조를 바꾸지 않는 범위에서 정보 우선순위로 반영합니다. */
  focusTopics: FocusTopic[];

  /** 문장 톤입니다. 대상 관람객과 목적에 맞는 말투를 결정합니다. */
  tone: Tone;

  /** 추가 기능 옵션입니다. PoC에서는 실제 외부 기능보다 UI 힌트/placeholder 수준으로 해석합니다. */
  featureOptions: FeatureOption[];

  /** 스토리텔링 템플릿일 때만 사용하는 시나리오입니다. 다른 템플릿에서는 무시하는 편이 안전합니다. */
  storyScenario?: StoryScenario;

  /** 사용자가 자유롭게 입력한 추가 요청입니다. 가장 직접적인 자연어 지시이므로 누락하지 말아야 합니다. */
  extraRequest: string;
}

/**
 * 기본 콘텐츠 생성기가 만드는 섹션입니다.
 *
 * 생성형 AI 레이아웃에서는 이 섹션을 그대로 카드 나열로 덤프하지 말고,
 * 템플릿 흐름에 맞게 재구성해야 합니다.
 */
export interface GeneratedSection {
  title: string;
  body: string;
  items?: string[];
}

/**
 * 서버 파일에 저장되는 생성 콘텐츠 레코드입니다.
 */
export interface GeneratedContent {
  /** 콘텐츠 ID이며 route와 generated layout registry의 key로 사용됩니다. */
  id: string;
  /** 관리자용 제목입니다. 방문자용 H1과 다를 수 있습니다. */
  title: string;
  /** 관리자 목록에서 보여줄 짧은 요약입니다. */
  summary: string;
  /** 게시 상태입니다. */
  status: ContentStatus;
  /** 최초 생성 시각입니다. */
  createdAt: string;
  /** 마지막 갱신 시각입니다. */
  updatedAt: string;
  /** 생성 요청 설정 원본입니다. */
  settings: ContentSettings;
  /** `settings`를 JSON 문자열로 저장한 값입니다. */
  settingsJson: string;
  /** 브라우저 hash route입니다. */
  routePath: string;
  /** 기본 생성기가 만든 보조 섹션입니다. 생성형 레이아웃에서는 재해석 대상입니다. */
  sections: GeneratedSection[];
}

/**
 * `frontend/public/mock_db.json`의 전체 구조입니다.
 */
export interface MockDatabase {
  /** 목업 DB 버전입니다. */
  version: string;
  /** DB 설명입니다. */
  description: string;
  /** 식물/나무 레코드 목록입니다. */
  plants: PlantRecord[];
}
